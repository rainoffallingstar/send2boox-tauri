{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the dashboard window.",
  "windows": [
    "dashboard"
  ],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
